// Types
import type * as Types from "shared/types";

// Components
import bottomMenu from "./bottomMenu";
import network from "./network";

const interfaceProps = {
	bottomMenu,
	network,
} as Types.InterfaceProps.default;

export = interfaceProps;
