// Packages
import Vide from "@rbxts/vide";

// Types
import type * as Types from "shared/types";
import Network from "@network/client";

// Vide Components
const { source } = Vide;

export default {
	visible: source(false),
} as Types.InterfaceProps.BottomMenu.default;
